$(function() {
	if ( $("#body").children().hasClass("home-latest-release") ) {

		// centerPlayButton_Featured();
		// $(window).on('resize', function(){
		// 	centerPlayButton_Featured();
		// });

	}
});
